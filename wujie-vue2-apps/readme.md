# 样式
  - wujie 底层利用 iframe，样式互不影响，但做了一些处理比 iframe 性能更好
  - 主题：父应用提供主题变量，通过 wujie 组件的生命周期注入到子应用

# 三方公用组件
  - 比如 Element，二次开发生成自己的 cdn，每个应用引用

# 跨应用调用弹窗类组件
  - 子应用通过 webpack5 Module Federation 暴露组件给主应用，在主应用中注册组件
    - 缺点：没加载子应用调组件，组件自己需要初始化需要的全局公用数据；
    - 缺点：这时候 window 是主应用的 window
  - 子应用调用父应用通过 props 注入（可以避免跨域问题）的 `window.$wujie.props.$app` 的 use 方法调用组件

# 跨应用引入局部组件
  - 引入其他应用暴露的 webpack5 联邦局部组件 button、item 这种，但会出现 $attrs $listeners 只读的报错警告，不影响使用

# 页面跳转
  - 子调父：利用父 $app 的 to 方法跳转页面
  - 父调子：子应用在主应用内始化后不要跳页面，等主应用调子应用 $app 的 to 方法跳转页面

# 数据变化后通知子应用
  - 所有应用的 $app 设计好 emit 和 on，通过事件和应用通信来更新数据